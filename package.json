{
  "name": "tactics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild js/game.js --bundle --format=esm --minify --outfile=game.js"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
